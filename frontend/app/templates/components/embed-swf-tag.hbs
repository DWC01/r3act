<embed class="embed-tag-element"
	type="application/x-shockwave-flash" 
	src="{{creative.resource_url}}"
	flashvars="{{flashvars}}" 
	width="{{creative.width}}" 
	height="{{creative.height}}" 
	allowscriptaccess="always"
	wmode="transparent">